[pytest]
testpaths = tests
filterwarnings =
    # starlette.testclient signale httpx comme déprécié ; sans incidence ici.
    ignore::DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*
